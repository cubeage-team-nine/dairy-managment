import api from '../../core/services/api.js'

export const dmiService = {
  /**
   * Placeholder service call to calculate DMI via backend API
   */
  async calculateDmiApi(payload) {
    // When backend endpoint is ready:
    // const response = await api.post('/dmi/calculate', payload)
    // return response.data
    return null
  },
}